import SwiftUI

struct SecondView: View {
    var body: some View {
        VStack {
            Text("نجرب")
                .font(.largeTitle)

        }
    }
}

struct SecondView_Previews: PreviewProvider {
    static var previews: some View {
        SecondView()
    }
}
